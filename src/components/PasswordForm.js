import React from 'react'

export const PasswordForm = () => {
  // Can save only if useTmi tmiStatus is TMI_STATUS.VALID
  // Or maybe if not, try a connection diractly...
  return <div>Password Form</div>
}
